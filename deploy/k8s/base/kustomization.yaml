apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# El Secret rclone-conf NO se genera desde aquí: se crea fuera de banda
# (kubectl create secret ... --from-file) — ver secret-rclone.example.yaml.
resources:
  - namespace.yaml
  - pvc.yaml
  - cronjob.yaml
